package genbytes

import "crypto/rand"

// Generate n cryptographically random bytes
func GenRandomBytes(n uint32) ([]byte, error) {
	b := make([]byte, n)
	_, err := rand.Read(b)
	if err != nil {
		return nil, err
	}

	return b, nil
}
